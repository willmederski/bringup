<div class="formcard">
	<p id="notice"><%= notice %></p>

	<p>
	  <b>Twilio:</b>
	  <%= @course.twilio_id %>
	</p>

	<p>
	  <b>Name:</b>
	  <%= @course.name %>
	</p>

	<%= render 'messages' %>


	<%= link_to 'Edit', edit_course_path(@course) %> |
	<%= link_to 'Back', courses_path %>
</div>
